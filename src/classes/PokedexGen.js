export default class PokedexGen {
  constructor (id, genName, name, pokemonList, totalNumOfPokemon) {
    this.id = id
    this.genName = genName
    this.name = name
    this.pokemonList = pokemonList
    this.totalNumOfPokemon = totalNumOfPokemon
  }
}
